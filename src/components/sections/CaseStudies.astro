---
const cases = [
  {
    tag: "B2B • SERVICIOS PROFESIONALES",
    headline: "De $20K a $3,000,000 en 18 meses",
    revenue: "$3M",
    roi: "150x",
    description: "Empresa B2B con tickets altos que necesitaba prospectos calificados, no curiosos. Implementamos un sistema de filtrado quirúrgico.",
  },
  {
    tag: "LEAD GEN • SEGUROS USA",
    headline: "Leads que sí contestan el teléfono",
    revenue: "+60%",
    roi: "3x",
    description: "Redujimos el costo por lead en un 40% mientras aumentamos la tasa de contacto real. Eficiencia pura en seguros USA.",
  },
  {
    tag: "E-COMMERCE • ESCALAMIENTO",
    headline: "ROAS 5.2x en solo 90 días",
    revenue: "+40%",
    roi: "5.2x",
    description: "Recuperamos el 23% de carritos abandonados solo con automatización AI. Lo que era estancamiento se convirtió en flujo.",
  }
];
---

<section class="py-32 bg-secondary relative overflow-hidden" id="casos">
  <div class="max-w-[1400px] mx-auto px-8">
    <div class="reveal max-w-4xl mb-20">
      <div class="inline-flex items-center gap-3 px-4 py-2 ultra-thin-border bg-white/5 text-white/40 text-[10px] font-bold uppercase tracking-[0.3em] rounded-full mb-8">
        <span class="w-1.5 h-1.5 rounded-full bg-accent"></span>
        Resultados Auditados
      </div>
      <h2 class="text-5xl md:text-7xl lg:text-8xl font-display font-black text-white leading-[0.9] tracking-tighter uppercase mb-6">
        Matemática <span class="font-serif-elite italic text-accent lowercase">Incontestable</span>
      </h2>
      <p class="text-xl md:text-2xl text-zinc-500 font-light max-w-2xl">
        No coleccionamos logos. Coleccionamos multiplicadores de capital.
      </p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
      {cases.map((caseItem, i) => (
        <div class="reveal group relative glass-card p-10 rounded-[2.5rem] transition-all duration-700 overflow-hidden" style={`transition-delay: ${i * 150}ms;`}>
          <!-- Decorative Background -->
          <div class="absolute -right-20 -top-20 w-48 h-48 rounded-full blur-[100px] opacity-10 group-hover:opacity-20 transition-opacity bg-accent"></div>
          
          <div class="relative z-10 flex flex-col h-full">
            <span class="text-[10px] font-bold uppercase tracking-[0.3em] text-white/30 mb-8 block">
              {caseItem.tag}
            </span>
            
            <h3 class="text-2xl md:text-3xl font-display font-black text-white mb-6 leading-tight uppercase group-hover:text-accent transition-colors">
              {caseItem.headline}
            </h3>
            
            <p class="text-zinc-500 text-lg mb-10 flex-grow group-hover:text-zinc-400 transition-colors">
              {caseItem.description}
            </p>
            
            <div class="grid grid-cols-2 gap-4 mt-8 pt-8 border-t ultra-thin-border">
              <div>
                <span class="block text-[9px] font-bold text-white/20 uppercase tracking-widest mb-1">Ventas</span>
                <span class="text-2xl font-display font-black text-white">{caseItem.revenue}</span>
              </div>
              <div>
                <span class="block text-[9px] font-bold text-white/20 uppercase tracking-widest mb-1">ROI / ROAS</span>
                <span class="text-2xl font-display font-black text-accent">{caseItem.roi}</span>
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- CTA centered -->
    <div class="reveal mt-20 text-center" style="transition-delay: 400ms;">
      <a href="/contacto" class="cta-elite-glow inline-flex items-center gap-4 text-xs font-bold uppercase tracking-[0.2em] px-12 py-6 rounded-full">
        ¿Podemos escalar tu negocio? Hablémoslo.
        <span class="material-symbols-outlined text-lg">arrow_outward</span>
      </a>
    </div>
  </div>
</section>
