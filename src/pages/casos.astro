---
import Layout from "../layouts/Layout.astro";
import Header from "../components/layout/Header.astro";
import Footer from "../components/layout/Footer.astro";
import CaseStudies from "../components/sections/CaseStudies.astro";
import CtaSection from "../components/sections/CtaSection.astro";

const summaryStats = [
  { value: "$10M+", label: "Generados para clientes" },
  { value: "50+", label: "Casos de éxito" },
  { value: "8-12x", label: "ROI promedio" },
  { value: "150x", label: "Mejor caso documentado" },
];

const industries = [
  "B2B SaaS",
  "Seguros",
  "Real Estate",
  "E-commerce",
  "Consultoría",
  "Educación Online",
  "Servicios Profesionales",
  "Manufactura",
];

const caseStudyLinks = [
  {
    title: "E-commerce B2B: $3M en 18 meses",
    href: "/casos/ecommerce-b2b",
    tag: "B2B • SERVICIOS",
  },
  {
    title: "Seguros USA: Leads que contestan",
    href: "/casos/seguros-usa",
    tag: "LEAD GEN • USA",
  },
];
---

<Layout title="Casos de Éxito | In Agencia">
  <Header />
  <main>
    <!-- Hero Section (Dark Stitch Style) -->
    <section
      class="relative min-h-[70vh] flex items-center pt-32 pb-20 overflow-hidden bg-secondary"
    >
      <div
        class="absolute top-[15%] left-[10%] w-[600px] h-[600px] glass-sphere rounded-full opacity-10 blur-lg"
      >
      </div>
      <div
        class="absolute bottom-[-10%] right-[5%] w-[800px] h-[800px] glass-sphere rounded-full opacity-5 blur-xl"
      >
      </div>

      <div class="max-w-[1400px] mx-auto px-8 relative z-10">
        <div class="max-w-4xl">
          <div
            class="reveal inline-flex items-center gap-3 px-4 py-2 mb-8 rounded-full ultra-thin-border bg-white/5 text-white/60 text-[10px] font-bold uppercase tracking-[0.3em]"
          >
            <span class="w-1.5 h-1.5 rounded-full bg-green-400 animate-pulse"
            ></span>
            Resultados Auditados
          </div>

          <h1
            class="reveal text-6xl md:text-8xl lg:text-9xl font-display font-black text-white mb-8 leading-[0.9] tracking-tighter uppercase mercury-text"
          >
            Resultados. <span class="font-serif-elite italic lowercase"
              >No promesas.</span
            >
          </h1>
          <p
            class="reveal text-xl md:text-2xl text-zinc-500 mb-12 leading-relaxed max-w-3xl font-light"
            style="transition-delay: 200ms;"
          >
            Aquí no hablamos de alcances o impresiones. Hablamos de ROI, ROAS y
            facturación neta. La matemática no miente.
          </p>
        </div>

        <!-- Quick Stats -->
        <div
          class="reveal grid grid-cols-2 md:grid-cols-4 gap-6 mt-12"
          style="transition-delay: 400ms;"
        >
          {
            summaryStats.map((stat) => (
              <div class="p-6 glass-card rounded-2xl text-center">
                <div class="text-3xl md:text-4xl font-display font-black text-white mb-2">
                  {stat.value}
                </div>
                <div class="text-white/30 text-[10px] uppercase tracking-[0.2em] font-bold">
                  {stat.label}
                </div>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- Industries Section -->
    <section class="py-16 bg-secondary border-t ultra-thin-border">
      <div class="max-w-[1400px] mx-auto px-8">
        <div class="text-center mb-12 reveal">
          <p
            class="text-[10px] font-bold text-white/20 uppercase tracking-[0.4em]"
          >
            Industrias donde hemos generado resultados
          </p>
        </div>
        <div class="flex flex-wrap justify-center gap-4 reveal">
          {
            industries.map((industry) => (
              <span class="px-6 py-3 glass-card text-white/60 font-bold rounded-full text-sm hover:bg-white/10 transition-colors cursor-default">
                {industry}
              </span>
            ))
          }
        </div>
      </div>
    </section>

    <!-- Featured Case Studies Links -->
    <section class="py-20 bg-secondary">
      <div class="max-w-[1400px] mx-auto px-8">
        <div class="reveal text-center mb-16">
          <h2
            class="text-4xl md:text-5xl font-display font-black text-white uppercase tracking-tight mb-4"
          >
            Casos Detallados
          </h2>
          <p class="text-zinc-500 text-xl font-light">
            Explora la estrategia completa detrás de cada resultado.
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
          {
            caseStudyLinks.map((study, i) => (
              <a
                href={study.href}
                class="reveal group glass-card p-10 rounded-[2.5rem] hover:bg-white/10 transition-all duration-500 block"
                style={`transition-delay: ${i * 100}ms;`}
              >
                <span class="text-[10px] font-bold uppercase tracking-[0.3em] text-white/30 mb-4 block">
                  {study.tag}
                </span>
                <h3 class="text-2xl md:text-3xl font-display font-black text-white group-hover:text-accent transition-colors mb-6">
                  {study.title}
                </h3>
                <div class="flex items-center gap-2 text-white/40 group-hover:text-white transition-colors">
                  <span class="text-sm font-bold uppercase tracking-wide">
                    Ver caso completo
                  </span>
                  <span class="material-symbols-outlined text-lg group-hover:translate-x-2 transition-transform">
                    arrow_forward
                  </span>
                </div>
              </a>
            ))
          }
        </div>
      </div>
    </section>

    <CaseStudies />

    <!-- Why no logos section -->
    <section class="py-24 bg-secondary">
      <div class="max-w-[1400px] mx-auto px-8">
        <div class="max-w-4xl mx-auto glass-card p-12 md:p-16 rounded-[3rem]">
          <div class="flex flex-col md:flex-row gap-12 items-center">
            <div
              class="w-24 h-24 shrink-0 rounded-2xl ultra-thin-border flex items-center justify-center text-5xl bg-white/5"
            >
              🤫
            </div>
            <div>
              <h2 class="text-3xl font-display font-black text-white mb-6">
                "¿Por qué no mostramos logos?"
              </h2>
              <div class="space-y-4 text-lg text-zinc-500">
                <p>
                  Muchas agencias muestran logos de multinacionales para parecer
                  importantes. Nosotros preferimos proteger la privacidad de
                  nuestros clientes y mostrar la <strong class="text-white"
                    >matemática detrás de cada éxito</strong
                  >.
                </p>
                <p>
                  Cada uno de estos casos tiene un dueño de negocio que, al
                  igual que tú, buscaba rentabilidad. Si quieres conocer los
                  detalles específicos de un sector, podemos discutirlos bajo
                  confidencialidad en nuestra llamada.
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <CtaSection />
  </main>
  <Footer />
</Layout>
